# URL do backend ZUME.
# Em dev local, pode deixar em branco (cai em http://localhost:3000 por padrão).
# Em produção, aponte para a URL do backend hospedado (ex: Render, Railway...).
VITE_API_URL=http://localhost:3000
